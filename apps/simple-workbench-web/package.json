{
  "name": "simple-workbench-web",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "node -e \"const http=require('http'); const port=process.env.PORT||5173; const html='<!doctype html><html><body><h1>Simple Workbench</h1></body></html>'; http.createServer((_,res)=>{res.writeHead(200,{'Content-Type':'text/html'});res.end(html);}).listen(port,'0.0.0.0');\"",
    "test": "vitest run --environment jsdom"
  },
  "dependencies": {
    "@simple-workbench/note-builder": "*"
  }
}
